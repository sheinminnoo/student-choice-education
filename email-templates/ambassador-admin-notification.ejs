<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif; background-color: #f4f4f4; margin: 0; padding: 0; }
    .container { max-width: 600px; margin: 20px auto; background: #ffffff; border-radius: 8px; overflow: hidden; border: 1px solid #e5e7eb; }
    .header { background-color: #020b2c; padding: 20px; color: #ffffff; }
    .header h2 { margin: 0; font-size: 20px; font-weight: 600; }
    .badge { background-color: #facc15; color: #020b2c; padding: 4px 10px; border-radius: 12px; font-size: 12px; font-weight: bold; float: right; margin-top: -24px; }
    .content { padding: 30px; }
    .field-row { margin-bottom: 12px; border-bottom: 1px solid #f3f4f6; padding-bottom: 12px; }
    .field-label { font-size: 12px; color: #6b7280; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; display: block; margin-bottom: 4px; }
    .field-value { font-size: 15px; color: #111827; font-weight: 500; }
    .motivation-box { background-color: #f9fafb; border: 1px solid #e5e7eb; border-radius: 6px; padding: 15px; margin-top: 20px; }
    .footer { background-color: #f9fafb; padding: 15px; text-align: center; font-size: 12px; color: #9ca3af; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h2>New Ambassador App</h2>
      <span class="badge">PENDING REVIEW</span>
    </div>
    
    <div class="content">
      <div class="field-row">
        <span class="field-label">Full Name</span>
        <div class="field-value"><%= fullName %></div>
      </div>

      <div class="field-row">
        <span class="field-label">Contact Info</span>
        <div class="field-value"><a href="mailto:<%= email %>" style="color:#2563eb; text-decoration:none;"><%= email %></a></div>
        <div class="field-value" style="margin-top:4px;"><%= phone %></div>
      </div>

      <div class="field-row">
        <span class="field-label">Languages Spoken</span>
        <div class="field-value"><%= languages %></div>
      </div>

      <div class="field-row">
        <span class="field-label">Social Profile</span>
        <div class="field-value">
           <% if (socialLink) { %>
             <a href="<%= socialLink.startsWith('http') ? socialLink : 'https://' + socialLink %>" target="_blank" style="color:#2563eb;">View Profile</a>
             <span style="font-size:12px; color:#666;">(<%= socialLink %>)</span>
           <% } else { %>
             Not provided
           <% } %>
        </div>
      </div>

      <div class="field-row">
        <span class="field-label">Location</span>
        <div class="field-value"><%= location %> (<%= postalCode %>)</div>
      </div>

      <div class="field-row">
        <span class="field-label">Current University</span>
        <div class="field-value"><%= study %></div>
      </div>

      <div class="field-row">
        <span class="field-label">Preferred Destination</span>
        <div class="field-value"><%= destination %></div>
      </div>

      <div class="motivation-box">
        <span class="field-label">Why they want to join:</span>
        <p style="margin: 8px 0 0 0; color: #374151; font-size: 14px; line-height: 1.6;">
          "<%= motivation %>"
        </p>
      </div>

      <div style="margin-top: 20px; padding: 10px; background-color: #ecfdf5; border: 1px solid #10b981; border-radius: 6px; color: #065f46; font-size: 14px; font-weight: 600; display: flex; align-items: center;">
        <% if (hasCV) { %>
          📎 A CV is attached to this email.
        <% } else { %>
          ⚠️ No CV was uploaded.
        <% } %>
      </div>
    </div>

    <div class="footer">
      Sent from Student Choice Education Website
    </div>
  </div>
</body>
</html>